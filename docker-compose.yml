services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ledgio
      POSTGRES_PASSWORD: ledgio
      POSTGRES_DB: ledgio
    ports:
      - "5432:5432"
    volumes:
      - ledgio_db_data:/var/lib/postgresql/data

volumes:
  ledgio_db_data:
