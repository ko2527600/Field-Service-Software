{
  "region": "eu-central-1",
  "app": {
    "name": "api",
    "root": "apps/api",
    "framework": "bun",
    "entry": "src/index.ts",
    "httpPort": 3000,
    "build": {
      "command": "npm run build",
      "outputDirectory": "."
    }
  }
}
